apiVersion: v2
name: bd-daemon
description: Helm chart for the bd (beads) daemon — Dolt database, RPC daemon, Slack bot, Redis, and NATS
type: application
version: 0.2.52
appVersion: "0.62.17"

dependencies:
  - name: redis
    version: "~20.x"
    repository: oci://registry-1.docker.io/bitnamicharts
    condition: redis.enabled
  # NATS is a native template (templates/nats-statefulset.yaml), not a subchart.
  # The bitnami chart's entrypoint has config-loading issues with ECR mirrors.
