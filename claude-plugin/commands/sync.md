---
description: "[REMOVED] Synchronize issues with git remote"
argument-hint: ""
---

**REMOVED:** `bd sync` has been removed. Dolt handles sync automatically.

- For manual export: use `bd export`
- For manual import: use `bd import`

No manual sync is needed in normal workflows.
